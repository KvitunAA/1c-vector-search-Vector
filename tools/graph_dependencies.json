{
  "name": "graph_dependencies",
  "description": "Анализ зависимостей: какие объекты ссылаются на указанный объект. Полезно для оценки влияния изменений (что затронет изменение объекта X).",
  "arguments": {
    "type": "object",
    "properties": {
      "object_name": {
        "type": "string",
        "description": "Имя объекта метаданных (например: 'Номенклатура', 'Сотрудники')"
      }
    },
    "required": [
      "object_name"
    ]
  }
}
